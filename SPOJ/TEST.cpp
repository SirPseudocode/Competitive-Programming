#include<bits/stdc++.h>
using namespace std;

void solve(){
    short in;
    while(cin >> in){
        if(in == 42) return;
        cout << in << '\n';
    }
}

int main(){
    ios_base::sync_with_stdio(0);
    cout.tie(0);    cin.tie(0);

    solve();
    return 0;
}