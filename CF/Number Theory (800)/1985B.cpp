#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    short t,n;
    cin >> t;
    while(t--){
        cin >> n;
        if(n == 3) cout << 3 << '\n';
        else cout << 2 << '\n';
    }
}