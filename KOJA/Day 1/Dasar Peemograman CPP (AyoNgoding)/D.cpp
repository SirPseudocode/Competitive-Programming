#include <bits/stdc++.h>
using namespace std;

void solve(){
    int h, a;
    cin >> h >> a;
    cout << (h + a - 1) / a << '\n';
    return;
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    solve();
}